{
  "name": "@dictex/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript for DicTeX apps: event schema, dataset/test_frozen format, CER/WER scoring. Empty scaffold (pivot Phase 1); later phases populate it.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
